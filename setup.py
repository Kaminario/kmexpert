from setuptools import find_packages, setup


setup(
    name='Kmexpert',
    version='0.1',
    packages=find_packages(),
    # scripts=['main_example'],
    install_requires=[
        "click",
    ]
)
